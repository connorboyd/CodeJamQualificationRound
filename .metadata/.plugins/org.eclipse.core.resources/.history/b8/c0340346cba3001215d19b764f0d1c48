import scala.io._

object TicTacTomekSolver {
  
  def readInput = {
    var fileName = "file.in"
    val s = Source.fromFile(fileName)
    s.getLines.foreach((line) => {
      println(line)
    })
    
  }

}